# Global Claude instructions

<!--
TODO: Port real content from Mac.

This file is loaded as global preamble for every Claude Code session on this
machine. Keep it short and load-bearing — anything in here consumes context on
every run.

Good things to put here:
- Preferred languages / frameworks / tooling
- Cross-project conventions (commit style, file naming, etc.)
- Hard "never do this" rules that apply everywhere
- Pointers to per-project CLAUDE.md files for richer context

Keep OUT:
- Secrets, API keys, tokens
- Anything project-specific (use a per-project CLAUDE.md instead)
- Work-confidential info (this repo is PUBLIC)
-->
