# External Publishing

- Pass user-visible bodies for GitHub comments, reviews, issues, and pull
  requests through structured tool arguments, standard input, or a file. Never
  interpolate Markdown or other untrusted text into a shell command; shell
  substitutions can execute content and silently alter the published message.
